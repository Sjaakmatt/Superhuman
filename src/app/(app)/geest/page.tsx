import { PagePlaceholder } from "@/components/page-placeholder";

export const metadata = { title: "Geest" };

export default function GeestPage() {
  return (
    <PagePlaceholder
      title="Geest"
      description="Meditaties, breathwork en journaling met stemming."
      phase="Breathwork komt in Fase 1"
      accent="var(--attr-geest)"
    />
  );
}
